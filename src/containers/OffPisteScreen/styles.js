import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
  searchField: {
    height: 40,
    width: '95%',
    borderWidth: 1,
    borderColor: 'gray'
  }
});
